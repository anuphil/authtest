div Books
div(ng-repeat="book in books") {{book}}
div.row.alert.animated(ng-if="!books", class="alert-{{alert-type}}", ng-class="{'flipInY': alert.show, 'flipOutY': !alert.show, 'alert-hidden': !alert.hasBeenShown}")
			strong{{alert.title}}
			{{alert.message}}